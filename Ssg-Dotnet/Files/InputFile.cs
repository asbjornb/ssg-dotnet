namespace Ssg_Dotnet.Files;

internal class InputFile : ContentFile
{
    public InputFile(string path, string fileName, string content, string extension) : base(path, fileName, content, extension)
    {
    }
}
